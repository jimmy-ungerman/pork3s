apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: truenas-slice
  labels:
    kubernetes.io/service-name: truenas
  namespace: networking
addressType: IPv4
ports:
  - name: http
    protocol: TCP
    port: 80
endpoints:
  - addresses:
      - "10.0.0.25"
    conditions:
      ready: true
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: truenas
  namespace: networking
  annotations:
    external-dns.alpha.kubernetes.io/target: porkboi.io
spec:
  ingressClassName: nginx
  rules:
    - host: "truenas.porkboi.io"
      http:
        paths:
          - backend:
              service:
               name: truenas
               port:
                 number: 80
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - "truenas.porkboi.io"
---
apiVersion: v1
kind: Service
metadata:
  name: truenas
  namespace: networking
spec:
  ports:
    - name: http
      port: 80
  type: ClusterIP