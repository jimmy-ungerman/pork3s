fullnameOverride: external-dns-unifi
logLevel: debug
provider:
  name: webhook
  webhook:
    image:
      repository: ghcr.io/kashalls/external-dns-unifi-webhook
      tag: main@sha256:f5f4283dab5163d8c4a2b9d5bb6e90fdf236a102619874c728111e487472f023
    env:
      - name: UNIFI_HOST
        value: "https://10.0.0.1"
      - name: SERVER_HOST
        value: "0.0.0.0"
      - name: UNIFI_USER
        valueFrom:
          secretKeyRef:
            name: &secret external-dns-unifi-secret
            key: UNIFI_USER
      - name: UNIFI_PASS
        valueFrom:
          secretKeyRef:
            name: *secret
            key: UNIFI_PASS
policy: sync
sources:
  - ingress
txtOwnerId: default
txtPrefix: k8s.
extraArgs:
  - --annotation-filter=external-dns.alpha.kubernetes.io/target
domainFilters:
  - porkboi.io
excludeDomains:
  - internal
serviceMonitor:
  enabled: false
podAnnotations:
  secret.reloader.stakater.com/reload: *secret