fullnameOverride: external-dns-unifi
logLevel: debug
provider:
  name: webhook
  webhook:
    image:
      repository: ghcr.io/kashalls/external-dns-unifi-webhook
      tag: main@sha256:0f4547658cf8edd7291c5a6c582a4b4935262f6d51818e5fc5a5836f65a10d54
    env:
      - name: UNIFI_HOST
        value: "https://10.0.0.1"
      - name: SERVER_HOST
        value: "0.0.0.0"
      - name: UNIFI_USER
        valueFrom:
          secretKeyRef:
            name: &secret external-dns-unifi-secret
            key: UNIFI_USER
      - name: UNIFI_PASS
        valueFrom:
          secretKeyRef:
            name: *secret
            key: UNIFI_PASS
policy: sync
sources:
  - ingress
txtOwnerId: default
txtPrefix: k8s.
extraArgs:
  - --annotation-filter=external-dns.alpha.kubernetes.io/target
domainFilters:
  - porkboi.io
excludeDomains:
  - internal
serviceMonitor:
  enabled: false
podAnnotations:
  secret.reloader.stakater.com/reload: *secret