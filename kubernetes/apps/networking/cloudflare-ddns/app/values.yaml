controllers:
  main:
    containers:
      main:
        image:
          repository: docker.io/oznu/cloudflare-ddns
          tag: latest
        resources:
          requests:
            cpu: 15m
            memory: 64M
          limits:
            cpu: 15m
            memory: 64M  

        env:
          PROXIED: "true"
          RRTYPE: "A"
          DELETE_ON_STOP: "false"
          DNS_SERVER: "1.1.1.1"
          TEST: "this is a test"
        envFrom:
          - secretRef:
              name: cloudflare-ddns-secret

service:
  main:
    enabled: false
ingress:
  main:
    enabled: false

