apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: usenet-data
  namespace: media
spec:
  storageClassName: ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi