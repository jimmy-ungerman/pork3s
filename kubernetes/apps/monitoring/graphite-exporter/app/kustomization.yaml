---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring

configMapGenerator:
  - name: graphite-exporter-configmap
    files:
      - ./config/graphite_mapping.yml
generatorOptions:
  disableNameSuffixHash: true