---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - app.yaml

configMapGenerator:
  - name: graphite-exporter-configmap
    files:
      - ./config/graphite_mapping.yaml
generatorOptions:
  disableNameSuffixHash: true