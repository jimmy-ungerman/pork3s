authentik:
  secret_key: "PleaseGenerateA50CharKey"
  error_reporting:
    enabled: true
  postgresql:
    host: postgres-rw


ingress:
  enabled: true
  hosts:
    - host: authentik.porkboi.io
      paths:
        - path: "/"
          pathType: Prefix

redis:
  enabled: true
