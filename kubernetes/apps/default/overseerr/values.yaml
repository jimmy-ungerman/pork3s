controller:
  type: statefulset
image:
  repository: ghcr.io/sct/overseerr
  tag: master@sha256:aa849776809dfe891e67ff4db6861ef44df1a774 
env:
  TZ: America/Boise
  LOG_LEVEL: "info"
  PORT: &port 5055

service:
  main:
    ports:
      http:
        port: *port

podSecurityContext:
  runAsUser: 568
  runAsGroup: 568
  fsGroup: 568
  fsGroupChangePolicy: "OnRootMismatch"

volumeClaimTemplates:
  - name: config
    mountPath: /app/config
    accessMode: ReadWriteOnce
    size: 1Gi
    storageClass: longhorn

ingress:
  main:
    enabled: true
    ingressClassName: nginx
    annotations:
      external-dns.alpha.kubernetes.io/target: porkboi.io
      cert-manager.io/cluster-issuer: le-prod
    hosts:
      - host: &host "requests.porkboi.io"
        paths:
          - path: /
            pathType: Prefix
    tls:
      - hosts:
          - *host


resources:
  requests:
    cpu: 10m
    memory: 250Mi
  limits:
    memory: 1500Mi