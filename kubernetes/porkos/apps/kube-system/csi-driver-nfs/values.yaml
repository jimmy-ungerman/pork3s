csi-driver-nfs:
  storageClass:
    create: true
    name: nfs-slow
    parameters:
      server: nas.porkboi.io
      share: /mnt/earth/Kubernetes
    mountOptions:
      - nfsvers=4.2
      - nconnect=16
      - hard
      - noatime
    reclaimPolicy: Delete
    volumeBindingMode: Immediate