apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-cert
  namespace: traefik
spec:
  secretName: traefik-cert
  dnsNames:
  - "jimmycodes.io"
  - "*.jimmycodes.io"
  issuerRef:
    name: le-prod
    kind: ClusterIssuer